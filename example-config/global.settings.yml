autolaunch_profile: example
load_order: 0
data_dir: .data
keybinds:
  block_actions:
    - action: rerun
      blocks:
        - build
        - assemble
      target: selected
      key: C
    - action: rerun
      blocks:
        - build
        - assemble
      target: all
      key: <shift>C
